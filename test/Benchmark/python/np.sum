# coding: utf8
import numpy as np

m = np.linspace(1, 20, 20).reshape(2, 10)

s = np.sum(m)
s = np.sum(m,axis = 0)
s = np.sum(m,axis = 1)
s = np.sum(m,axis = 0, keepdims=True)
s = np.sum(m,axis = 1, keepdims=True)
