# coding: utf8
import numpy as np

m = np.linspace(1, 20, 20).reshape(2, 10)

s = m.sum()
s = m.sum(axis = 0)
s = m.sum(axis = 1)
s = m.sum(axis = 0, keepdims=True)
s = m.sum(axis = 1, keepdims=True)
